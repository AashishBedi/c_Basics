// WAP to check whether a number is divisible by 2 or not without using if-else

#include<stdio.h>
#include<math.h>
int main()
{
	int a;
	printf("a: ");
	scanf("%d",&a);
	printf("%d \n", a%2 == 0);
	return 0;
}
